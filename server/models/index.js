const User = require('./User');
const League = require('./League');
const Match = require('./Match');
const Team = require('./Team');
const Tournament = require('./Tournament');

module.exports = { User, League, Match, Team, Tournament};
